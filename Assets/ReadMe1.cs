using UnityEngine;
using System.Collections;

public class ReadMe1 : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	//Player finds a cliff and gets an UI message on how to do a single jump which they must do to get higher.
	//Just make sure you can jump for this area for scripting.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
