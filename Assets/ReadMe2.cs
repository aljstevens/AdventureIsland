using UnityEngine;
using System.Collections;

public class ReadMe2 : MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
	//Player finds a higher cliff and gets an UI message on how to do a hold jump which they must do to get higher.
	//Just make sure you can do a hold jump for this area for scripting.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
