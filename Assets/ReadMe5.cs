using UnityEngine;
using System.Collections;

public class ReadMe5: MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
		//Player reaches a climbable cliffside (Perhaps vines or something fancy like that?).
		//Just make sure you can climb objects for scripting.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
