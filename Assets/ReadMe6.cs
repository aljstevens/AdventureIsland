using UnityEngine;
using System.Collections;

public class ReadMe6: MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
		//Player reaches a bunch of pilers or something along the lines of that. They have to jump onto them in order to make it to the otherside.
		//Just make sure that some pilers can break apart but not all of them, we don't want the path to get across to get lost forever.
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
