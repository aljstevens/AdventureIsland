using UnityEngine;
using System.Collections;

public class ReadMe8: MonoBehaviour {

	// Use this for initialization
	void Start ()
	{
		//Player finds a door which blocks them and they have to unlock it by enteracting with switches that change maybe the colour or image on an object.
		//Just make sure you have objects that the player can enteract with and have either an object changes it's colour or it's image or the way it's facing.
		//Perhaps have the door requires three bools to equal true to open?
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
